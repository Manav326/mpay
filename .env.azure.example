# Azure VM deployment environment.
# Copy this file to .env on the VM and replace the placeholders.
#
# The VM deployment uses PostgreSQL in Docker; no Azure-managed PostgreSQL is required.

MPAY_BACKEND_IMAGE=ghcr.io/manav326/mpay-backend
MPAY_ADMIN_WEB_IMAGE=ghcr.io/manav326/mpay-admin-web

# Exact backend image from the Git commit being deployed.
MPAY_IMAGE_TAG=sha-REPLACE_WITH_COMMIT_SHA

# Azure-specific Admin Web image built with the public API URL.
MPAY_ADMIN_WEB_TAG=azure-sha-REPLACE_WITH_COMMIT_SHA

MPAY_DB_NAME=recharge
MPAY_DB_USERNAME=recharge
MPAY_DB_PASSWORD=REPLACE_WITH_STRONG_DB_PASSWORD

MPAY_WEB_API_BASE_URL=https://api.mpay.thinkwithsujeet.in
MPAY_CORS_ALLOWED_ORIGINS=https://mpay.thinkwithsujeet.in

MPAY_PUBLIC_DOMAIN=mpay.thinkwithsujeet.in
MPAY_API_DOMAIN=api.mpay.thinkwithsujeet.in

MPAY_RENTAL_PLATFORM_FEE_PERCENT=10.00

# Keep application services private to the VM; Caddy is the only public entry point.
MPAY_BACKEND_BIND_ADDRESS=127.0.0.1
MPAY_ADMIN_WEB_BIND_ADDRESS=127.0.0.1
