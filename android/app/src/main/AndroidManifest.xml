<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- PayU Checkout Pro declares a default application label; explicitly replace it with mPay. -->
    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.RechargeClient"
        android:label="mPay"
        android:icon="@drawable/mpay_logo"
        android:roundIcon="@drawable/mpay_logo"
        tools:replace="android:theme,android:allowBackup,android:label">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
