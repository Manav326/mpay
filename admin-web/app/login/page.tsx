'use client';
import { FormEvent,useState } from 'react';
import { ArrowLeft,ShieldCheck,UserPlus } from 'lucide-react';
const base=process.env.NEXT_PUBLIC_API_BASE_URL?.replace(/\/$/,'')||'http://localhost:8080';
export default function UserLogin(){
 const [mobile,setMobile]=useState(''),[password,setPassword]=useState(''),[busy,setBusy]=useState(false),[notice,setNotice]=useState('');
 async function submit(e:FormEvent){e.preventDefault();setBusy(true);setNotice('');try{const r=await fetch(base+'/api/v1/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({mobile,password})});if(!r.ok)throw new Error((await r.text())||'Login failed');const d=await r.json();localStorage.setItem('mpay_token',d.accessToken);localStorage.setItem('mpay_refresh_token',d.refreshToken);window.location.href='/portal';}catch(e:any){setNotice(e.message||'Unable to sign in.')}finally{setBusy(false)}}
 return <main className="portal-auth-page"><section className="portal-auth-card"><a href="/" className="back-link"><ArrowLeft size={16}/> Back to mPay</a><div className="admin-login-brand"><img src="/mpay-logo.png" alt="mPay"/><div><strong>mPay</strong><span>Customer account</span></div></div><div className="admin-login-copy"><div className="secure-badge"><ShieldCheck size={15}/> Secure sign in</div><h1>Welcome back.</h1><p>Sign in to recharge, manage your wallet and keep track of every transaction.</p></div><form className="form" onSubmit={submit}><label>Mobile number<input inputMode="numeric" value={mobile} onChange={e=>setMobile(e.target.value)} placeholder="10-digit mobile number" autoComplete="username"/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Enter password" autoComplete="current-password"/></label>{notice&&<div className="alert">{notice}</div>}<button className="primary" disabled={busy}>{busy?'Signing in…':'Sign in'}</button></form><a className="auth-alt-link" href="/signup"><UserPlus size={15}/> New to mPay? Create an account</a></section></main>;
}