'use client';
import { FormEvent,useState } from 'react';
import { ArrowLeft,CheckCircle2 } from 'lucide-react';
const base=process.env.NEXT_PUBLIC_API_BASE_URL?.replace(/\/$/,'')||'http://localhost:8080';
export default function Signup(){
 const [name,setName]=useState(''),[email,setEmail]=useState(''),[mobile,setMobile]=useState(''),[password,setPassword]=useState(''),[busy,setBusy]=useState(false),[notice,setNotice]=useState('');
 async function submit(e:FormEvent){e.preventDefault();setBusy(true);setNotice('');try{const r=await fetch(base+'/api/v1/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:name||null,email:email||null,mobile,password})});if(!r.ok)throw new Error((await r.text())||'Registration failed');const d=await r.json();localStorage.setItem('mpay_token',d.accessToken);localStorage.setItem('mpay_refresh_token',d.refreshToken);window.location.href='/portal';}catch(e:any){setNotice(e.message||'Unable to create account.')}finally{setBusy(false)}}
 return <main className="portal-auth-page"><section className="portal-auth-card"><a href="/" className="back-link"><ArrowLeft size={16}/> Back to mPay</a><div className="admin-login-brand"><img src="/mpay-logo.png" alt="mPay"/><div><strong>mPay</strong><span>Create your account</span></div></div><div className="admin-login-copy"><div className="secure-badge"><CheckCircle2 size={15}/> Quick registration</div><h1>Start with mPay.</h1><p>Create your account and access your payments and recharge workspace.</p></div><form className="form" onSubmit={submit}><label>Name<input value={name} onChange={e=>setName(e.target.value)} placeholder="Your name"/></label><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><label>Mobile number<input inputMode="numeric" value={mobile} onChange={e=>setMobile(e.target.value)} placeholder="10-digit mobile number"/></label><label>Password<input type="password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)} placeholder="At least 8 characters"/></label>{notice&&<div className="alert">{notice}</div>}<button className="primary" disabled={busy}>{busy?'Creating account…':'Create account'}</button></form><a className="auth-alt-link" href="/login">Already have an account? Sign in</a></section></main>;
}